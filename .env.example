# Environment Variables for AMYGA Governance
# Copy this file to .env and configure as needed
# Note: The CLI does not auto-load .env files; use `dotenv` or export manually

# Override the default governance profile (creative|delivery|release)
# GOVERNANCE_PROFILE=delivery

# Maximum age in days for standards.versions.json freshness check
# STANDARDS_MAX_AGE_DAYS=90

# Fallback gitleaks version when detection fails
# GITLEAKS_VERSION=8.24.0
