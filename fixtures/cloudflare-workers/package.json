{
  "name": "fixture-cloudflare-workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "build": "wrangler build"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
