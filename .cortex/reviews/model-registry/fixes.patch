--- a/packages/mcp-server/src/config/model-registry.ts
+++ b/packages/mcp-server/src/config/model-registry.ts
@@ -99,7 +99,7 @@
      */
     static getInstance(
-        configPath: string = '/Users/jamiecraik/.Cortex-OS/config/ollama-models.json',
+        configPath: string = process.env.CORTEX_MODELS_CONFIG ?? resolve(process.env.CORTEX_REPO_ROOT ?? process.cwd(), 'config/ollama-models.json'),
         logger?: Logger,
     ): ModelRegistry {
         if (!ModelRegistry.instance) {
@@ -127,7 +127,6 @@
         const candidates = [
             process.cwd(),
             resolve(process.cwd(), '..'),
             resolve(process.cwd(), '..', '..'),
             process.env.CORTEX_REPO_ROOT,
-            '/Users/jamiecraik/.Cortex-OS',
         ].filter(Boolean) as string[];

--- a/packages/mcp-server/src/config/hybrid.ts
+++ b/packages/mcp-server/src/config/hybrid.ts
@@ -171,7 +171,12 @@
         if (defaultModels.length > 0) {
             return defaultModels;
         }
-    } catch {
+    } catch (error) {
+        logger?.debug(
+            createBrandedLog('model_registry_fallback', { error: String(error) }),
+            'ModelRegistry unavailable, using legacy config'
+        );
         // Fall through to legacy config
     }

@@ -192,7 +197,12 @@
         if (model) {
             return model.ollama_model;
         }
-    } catch {
+    } catch (error) {
+        logger?.debug(
+            createBrandedLog('model_registry_fallback', { error: String(error) }),
+            'ModelRegistry unavailable, using legacy config'
+        );
         // Fall through to legacy config
     }